# Base URL for the FastAPI backend service
# In local development, this defaults to the local backend.
# In production, set this to your deployed Railway container URL.
VITE_API_BASE_URL=http://localhost:8000
